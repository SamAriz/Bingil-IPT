# Bingil-IPT-sampl
-If the file have an error have an error
the common problem would be int the launchSettings.json
on the properties folder, to fix this error change the 
number at the end of this code
"applicationUrl": "http://localhost:5208"

